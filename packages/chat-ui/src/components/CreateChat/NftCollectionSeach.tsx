import React from "react";
import { Box } from "@chakra-ui/react";

interface INftCollectionSearchProps {}

export const NftCollectionSearch: React.FC<INftCollectionSearchProps> = (
  props
) => {
  return <Box>NftCollectionSearch</Box>;
};
