{
  "name": "@wum.bo/wumbo-programs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "Wum.bo Token Staking tests",
  "license": "MIT",
  "type": "module",
  "author": "Wum.bo Inc.",
  "devDependencies": {
    "@project-serum/anchor": "^0.13.2",
    "@types/mocha": "^9.0.0",
    "@wum.bo/spl-token-staking": "^1.0.0",
    "chai": "^4.3.4",
    "concurrently": "^6.2.1",
    "mocha": "^9.0.3",
    "ts-mocha": "^8.0.0",
    "ts-node": "^10.2.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "idl": "node app/idlToTs",
    "watch": "concurrently --kill-others \"lerna run --parallel watch\" \"yarn run idl\"",
    "test": "env ANCHOR_PROVIDER_URL=$ANCHOR_PROVIDER_URL ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
  },
  "dependencies": {
    "@project-serum/common": "^0.0.1-beta.3",
    "quicktype-core": "^6.0.70"
  }
}
